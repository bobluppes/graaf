# Public API documentation comments

Document what a public API does and how to use it: its purpose, parameters, return value, preconditions, and error behavior.

Do not expose implementation details of the algorithm or data structures in these comments (e.g. traversal strategy, internal containers, complexity that depends on internals). Those belong in the implementation, and a comment there must still follow the rules in `comments.md`.
